# Loop Board — tsd-modernization.com

The persistent State for the improvement loop. See [`loop.md`](loop.md). Agents append to **Inbox**; the verifier promotes to **Backlog** or **Rejected**; the implementer moves approved items to **In Review**; Nash moves merged items to **Shipped**.

Seeded by **Cycle 1 (2026-06-27)**: 7 lenses, an independent verifier on every finding, 42 confirmed, 1 rejected. **Cycles 2-3 (2026-06-27)** implemented 6 more safe wins from the backlog.

---

## In Review — branch `loop/cycle-2026-06-27` (awaiting Nash)

Verified, safe-to-automate wins, each `npm run build` green and runtime-checked. No production deploy until you merge.

### Cycle 1
- [x] **Fit-call length now says 30 minutes on Process** — correctness/copy — `src/pages/Process.jsx`
- [x] **Nav dropdown a11y: aria-haspopup/aria-controls + Escape returns focus** — accessibility — `src/Layout.jsx`
- [x] **Canonical / og:url / JSON-LD / sitemap / robots → www host** — seo + correctness — `Layout.jsx`, `route-jsonld.js`, `index.html`, `robots.txt`, `generate-sitemap.mjs`
- [x] **Stats grid breakpoint 760px → 768px** — design-ux — `src/pages/Home.jsx`
- [x] **Hero poster img/video intrinsic width/height (CLS safeguard)** — performance — `src/pages/Home.jsx`

### Cycle 2
- [x] **Hero autoplay respects prefers-reduced-motion** — accessibility — `src/pages/Home.jsx`
- [x] **@sentry/react dynamic-imported, out of the main bundle** — performance — `src/sentry.js`, `src/components/RootError.jsx`
- [x] **Google Fonts trimmed (dropped unused Cormorant + italic axes)** — performance — `index.html`

### Cycle 3
- [x] **Hero LCP poster preloaded per viewport + fetchpriority=high** — performance — `index.html`, `src/pages/Home.jsx`
  - Responsive preload links warm the correct poster before the JS chunk parses; fetchpriority flags it as the LCP image. (Caught and fixed a React 18 fetchPriority→fetchpriority prop bug via the runtime check.)
- [x] **Google Fonts now load non-blocking** — performance — `index.html`
  - media=print + onload swap with a noscript fallback, so the font request never delays first paint.
- [x] **Sitemap excludes noindexed /sheets/* and the /columbia-demo** — seo — `scripts/generate-sitemap.mjs`
  - Dropped 6 URLs that should not be indexed (27 → 21 routes). lastmod values are a minor residual left on the backlog.

> Deferred on purpose (need your judgment): the Contact-form CTA/label change (conversion); the single-`<h1>`-per-page SEO fix (page titles render via SectionHeader on some pages and a custom `EditorialMasthead` on Pricing, so 'which heading is canonical' is a per-page call); and the soft-404 status code (needs a Vercel-level 404, not just a code change). All on the Backlog.

## Backlog — verified, ranked by impact-to-effort

### High impact

- [ ] **Hero subhead is a single dense run-on paragraph that buries the proof point** — design-ux · high/S · needs judgment
  - Fix: Split into two short stacked lines: a one-line promise ('We find the money your business is already leaking and build what stops it.') and a one-line proof ('One Charlotte bakery was losing $540 a month until we cut it.'). Drop the audience and ownership clauses here since the cohort strip and…
  - Evidence: Home.jsx:176-183 is a single <p> at fontSize 16px, maxWidth 580px, margin '0 auto 20px', with no internal breaks. Verified word count is 65 (script run). The bolded span at line 182 is 'Priced to…
- [ ] **The primary "Book a fit call" CTA is hidden on mobile — buried two taps deep in a dropdown** — conversion · high/S · needs judgment *(verifier-adjusted)*
  - Fix: Add a persistent mobile booking affordance. Cheapest: drop the hide-mobile class so the nav "Book a fit call" stays visible (collapse to a short label/icon under 360px, where .site-nav-brand-text already hides per Layout.jsx:450-452, to fit alongside the Menu button). Better: a slim sticky bottom…
  - Evidence: Confirmed: Layout.jsx:594 wraps the nav "Book a fit call" in className="hide-mobile"; Layout.jsx:431-432 hides it under @media (max-width:768px). The only fixed elements left on mobile are…
- [ ] **The Book page strips out the risk-reversal and offer cues right where commitment is decided** — conversion · high/S · needs judgment
  - Fix: Add a compact trust strip directly under or beside the calendar repeating "Free 30-min call · Free cost-cut audit you keep · 48-hour written proposal · 100% money-back guarantee" (reuses copy already live on Contact.jsx:240, so no new claims). Add a Calendly-failure fallback: after a bounded number…
  - Evidence: Confirmed: Book.jsx:66-83 covers the audit and "written proposal within 48 hours" in prose but renders no guarantee/48hr trust badge near the embed; the GuaranteeBlock at Pricing.jsx:57-58 ("every…
- [ ] **Homepage hero subhead is a run-on comma-spliced list that reads machine-generated** — copy-voice · high/M · needs judgment
  - Fix: Rewrite as 2-3 plain sentences. Set the three leak nouns off cleanly (a real list, em-dash-free) and stop splicing 'and build' onto them. Lead with the leak promise; let the bakery number stand as its own short sentence. Keep 'Priced to your size, managed by us or owned by you.' This is core hero…
  - Evidence: Confirmed at Home.jsx:182: 'We find the money your business is already leaking, missed calls, slow quotes, forgotten subscriptions, and build what stops it with the same AI the big players run. One…
- [ ] **Entire site ships as one 512KB (150KB gzip) JS chunk — no route-level code splitting** — performance · high/M · needs judgment
  - Fix: Convert non-home route components to React.lazy() with dynamic imports so Vite emits per-route chunks (vite-react-ssg supports lazy routes and still prerenders them). At minimum lazy-load the three heaviest off-home leaf components: ChatbotDemo, MakeFlowDemo, PricingEstimator. Expect homepage…
  - Evidence: Verified build output: dist/assets/app-C9nhClpO.js is exactly 524087 bytes raw, 150214 bytes gzipped (I ran gzip -c | wc -c). dist/assets/ contains only 2 .js files (the app chunk + a 568-byte client…
- [ ] **Zero social proof on the entire landing-to-booking path; Testimonials is a "coming soon" page** — conversion · high/M · needs judgment *(verifier-adjusted)*
  - Fix: Get one real, attributed proof element onto the homepage and the Book page: a single first-name + business-type + one-line outcome (e.g. the bakery, named or concretely pseudonymized). Until Vol. 1 has a real entry, consider pulling "Testimonials" from the primary nav so high-intent visitors are…
  - Evidence: Confirmed: Testimonials.jsx:61 H1 "Case studies are coming soon."; Testimonials.jsx:95-137 three dashed "Coming soon" placeholder rows; ROUTE_META "/testimonials" title "Case Studies Coming Soon"…
- [ ] **Gradient-clipped accent text fails AA contrast in light mode (carolina→carolinaLight on cream)** — accessibility · high/M · needs judgment
  - Fix: Introduce a per-theme --c-gradient-accent CSS var set in both [data-theme] blocks in Layout.jsx; light mode should use a darker steel-anchored ramp (e.g. steel #2c5f8a, which measures ~5.9:1 on cream, ramping toward carolina) with every stop at or above 3:1 for these large runs (4.5:1 is safer…
  - Evidence: src/shared.jsx:27 defines gradientAccent. Consumed theme-blind at src/pages/Home.jsx:508 (stat), :706 (LeakCard), :803 (step numeral), :646 & :755 (GradientText headlines), src/pages/Book.jsx:61…
- [ ] **Six core indexable pages render zero <h1> — main page title ships as an <h2>** — seo · high/M · needs judgment
  - Fix: Give each of the six pages exactly one <h1> carrying its primary keyword. Add an optional level/as prop (or isPageTitle flag) to SectionHeader so the lead header on a page renders <h1> instead of <h2>, then pass it on the top header of each page; demote rather than add a second heading. Keep copy…
  - Evidence: grep -c '<h1' on all six page sources = 0. Live: curl https://www.tsd-modernization.com/pricing | grep -c '<h1' = 0. shared.jsx:443 SectionHeader renders a hardcoded <h2> (signature is { label, num,…
- [ ] **Every service detail page ships visible "Coming Soon" and "Placeholder" badges to prospects** — correctness · high/M · needs judgment
  - Fix: Gate the VideoCard 'Coming Soon' badge and the GallerySlideshow 'Placeholder' badge behind a real-asset check, or hide the Demo Videos / Solutions Gallery sections until real assets exist, so an unfinished tile is never shown to a buyer. Note ServiceDetail.jsx:464-469 already prints a softer 'Video…
  - Evidence: src/pages/ServiceDetail.jsx:61 unconditional 'Coming Soon' span inside VideoCard, rendered at line 462 for every service.videos entry; ServiceDetail.jsx:132 'Placeholder' span in the GallerySlideshow…
- [ ] **Em dashes saturate the copy despite a hard NO-em-dash rule, including SEO titles and meta descriptions** — copy-voice · high/L · needs judgment
  - Fix: Do a reviewed pass replacing em dashes in all VISIBLE copy and SEO/structured-data strings: Layout.jsx ROUTE_META titles/descriptions, route-jsonld.js, index.html JSON-LD/OG/Twitter, services-data.js, relationships-data.js, news-data.js, and every page headline/subhead/body. Use periods, commas, or…
  - Evidence: Confirmed in source: Layout.jsx:36 home meta description '...the money your business is leaking — missed after-hours calls, slow quotes, forgotten subscriptions — and build...'; Layout.jsx:43 title…

### Medium impact

- [ ] **Banned three-part closer in the News post and 'X, not Y' contrastive construction in service copy** — copy-voice · medium/S · needs judgment *(verifier-adjusted)*
  - Fix: Rewrite the News closer to drop the tricolon (state the pattern in one or two flat sentences). Recast the 'not X' tails as positive statements: e.g. 'It shows its sources on every answer.' and 'Answers come from vector search over the real docs.' While in the file, also fix the Home.jsx:725…
  - Evidence: Confirmed: news-data.js:59 closer 'That's the pattern. Build what pays for itself, run it on our own business first, then show the numbers.' (the file's own header at news-data.js:11 explicitly bans…
- [ ] **Apex→www redirect is a 307 (temporary), not a 308/301 permanent** — seo · medium/S · needs judgment
  - Fix: Make the host redirect permanent (308 to preserve method, or 301). Easiest path is Vercel Domains settings (set apex to permanently redirect to www); alternatively add an explicit host-matched permanent:true redirect in vercel.json. Coordinate with finding 1 so both agree on www as the host of…
  - Evidence: curl -s -o /dev/null -w '%{http_code} -> %{redirect_url}' https://tsd-modernization.com/pricing returned '307 -> https://www.tsd-modernization.com/pricing'. vercel.json defines explicit…
- [ ] **/testimonials is indexable and in the sitemap but is an explicit 'coming soon' placeholder** — seo · medium/S · needs judgment
  - Fix: Either populate it now with real case-study content (promote the bakery and importer results that already exist in copy and JSON-LD, ideally as Review/CreativeWork schema), or add a temporary noindex via the /testimonials <Head> in Layout.jsx and drop it from the sitemap until populated. This needs…
  - Evidence: src/Layout.jsx:95-96 ROUTE_META['/testimonials'] title 'Case Studies Coming Soon | ...' and description 'We're shipping our first client projects now ... coming soon.' sitemap.xml lines 128-132 list…
- [ ] **LeakCard is a static glass panel while its sibling card rows on the same page lift on hover** — design-ux · medium/M · needs judgment
  - Fix: Give LeakCard the same subtle hover lift the sibling cards use, or (cleaner) replace the hand-rolled LeakCard and WhyCard divs with the shared <Card> primitive so hover/sheen/rim behavior is identical across the page. Note WhyCard carries three bespoke per-index headers, so a full <Card> swap there…
  - Evidence: LeakCard (Home.jsx:682-715) is a plain div: no useState/hovered, no onMouseEnter, no transform or glow. WhyCard (Home.jsx:787-884) has hovered state (789), onMouseEnter/Leave (849-850),…
- [ ] **Hero masthead and per-vertical mastheads default to generic 'Web + AI Studio / Built Your Way' that dilutes the money-saver wedge** — copy-voice · medium/S · needs judgment
  - Fix: Swap the generic middle/last masthead tokens for wedge-bearing language that survives a competitor-swap test (e.g. 'We find the leak' or 'Money-back, managed or owned') instead of 'Web + AI Studio' / 'Built Your Way'. This is a positioning decision on a §4/§8.2 plan-governed surface, so make it a…
  - Evidence: Confirmed: Home.jsx:155 EditorialMasthead items={['Charlotte Metro', 'Web + AI Studio', 'Built Your Way']}; RelationshipPage.jsx:51 items={['Custom Website + AI', rel.vertical, 'Built Your Way']};…
- [ ] **The hero offers four competing CTAs, splitting click intent at the moment of highest attention** — conversion · medium/S · needs judgment
  - Fix: Demote the secondary: keep "Book a fit call" as the dominant action and render "See what you're losing" as a quieter ghost/text link rather than a same-weight editorial button (the codebase already has a ghost variant, used at Home.jsx:928). Keep /audit as the single low-commitment fallback.…
  - Evidence: Confirmed: Home.jsx:188-190 BookCallButton variant="primary"; Home.jsx:191-195 Link to /savings rendering Button variant="editorial" size="lg" "See what you're losing"; Home.jsx:204-220 the third <a…
- [ ] **Pricing estimator presents PLACEHOLDER dollar figures to live visitors as authoritative ranges** — conversion · medium/M · needs judgment *(verifier-adjusted)*
  - Fix: Ratify the numbers or align them to delivered deals before they keep anchoring prospects (Bisque $5,200 + $73/mo, the $49/mo managed site, the $73/mo-per-tier ladder already encoded in MANAGED at line 37). Per .claude/CLAUDE.md this needs a paired BUSINESS_PLAN §6 Pricing & §12 Financial Model…
  - Evidence: Confirmed: PricingEstimator.jsx:14-17 header "⚠️ PLACEHOLDER NUMBERS. Every figure in PRICING/SIZES/MANAGED is a default pending Nash's sign-off."; PRODUCTS website low:2900/high:4000 at line 30;…
- [ ] **Contact form asks for a free-text "message" and labels the submit "Send" — friction and a weak action verb at the conversion point** — conversion · medium/S · auto-safe
  - Fix: Rename the submit to an outcome verb mirroring the offer, e.g. "Get my free fit call" or "Start my project" (matches the "Start a project" CTA already used in the nav dropdown Layout.jsx:704 and Testimonials.jsx:183, so it stays consistent). Soften the textarea: reduce default height and relabel…
  - Evidence: Confirmed: Contact.jsx:219-223 textarea with minHeight 140px and placeholder "Tell us about your business and what you're looking to modernize..."; Contact.jsx:233-235 Button reads {sending ?…
- [ ] **Contact form fields have only sr-only labels plus a sub-4.5:1 placeholder, leaving the visible field identity hard to read** — accessibility · medium/M · needs judgment
  - Fix: Add persistent visible labels above each input (or a floating-label pattern) so the field identity survives typing and autofill. Separately raise the placeholder/dim contrast: a light-mode --c-text-dim of navy@0.72 clears 4.5:1 on every cream tier (5.0–5.6:1). At minimum lift the placeholder…
  - Evidence: src/pages/Contact.jsx:193/201/210/218 each pair a className="sr-only" label with a placeholder-only input; the bot honeypot at line 187 is not a real field, so 'four inputs' is correct. inputStyle at…
- [ ] **Unknown URLs return HTTP 200 with the homepage (soft-404) instead of a 404** — correctness · medium/M · needs judgment
  - Fix: Add a dedicated NotFound component for the * route (clear 'page not found' UI with a link home) instead of Home, register it in vite-react-ssg's static paths, and configure Vercel so unmatched routes serve it with a 404 status. This stops soft-404 indexing and gives lost visitors an honest signal.
  - Evidence: src/routes.jsx:76 { path: "*", Component: Home }; vercel.json last rewrite { "source": "/(.*)", "destination": "/index.html" }. Live: curl -sI…
- [ ] **Public pricing estimator and JSON-LD Offer prices are flagged placeholders pending sign-off** — correctness · medium/M · needs judgment *(verifier-adjusted)*
  - Fix: This is a business sign-off action, not a code defect: get the four SKU low/high figures, the MANAGED tiers, and OWNED_MULT confirmed, then remove the PLACEHOLDER warnings and update PricingEstimator.jsx, the app's estimator.ts, and route-jsonld.js together. Do NOT change any number without that…
  - Evidence: src/components/PricingEstimator.jsx:14, 26, 42 carry the PLACEHOLDER / KEEP IN SYNC warnings; route-jsonld.js:279-281 notes the minPrice floors mirror the estimator lows (placeholders, change…
- [ ] **Two floating dock widgets occupy both bottom corners on every page with no mobile suppression or safe-area inset** — design-ux · medium/M · needs judgment
  - Fix: Add bottom: calc(22px + env(safe-area-inset-bottom)) to both so neither pill sits under the iOS home indicator (low-risk, ship first). Separately consider collapsing one widget to an icon-only FAB on mobile (or suppressing the 'Call us' pill there since the nav and footer already expose the tel…
  - Evidence: Layout.jsx:893-894 mounts <TSDAgent /> and <CallButton /> globally. CallButton.jsx:20-24 is position:fixed bottom:22px left:22px zIndex:1000. TSDAgent.jsx:516-519 collapsed bubble is position:fixed…
- [ ] **Two third-party scripts (Calendly widget.js + elu.dev tag) and a render-blocking Calendly stylesheet load on every page in <head>** — performance · medium/M · needs judgment
  - Fix: Load Calendly's widget.css + widget.js on demand the first time a BookCallButton popup opens or only on the /book route, rather than globally in <head>; at minimum stop shipping the blocking widget.css in <head>. This requires writing a small idempotent loader and updating BookCallButton.jsx +…
  - Evidence: index.html:142 '<link rel="stylesheet" href="https://assets.calendly.com/assets/external/widget.css" />' (blocking), :143 widget.js with async, :146 elu.dev async tag. Confirmed in prerendered…

### Low impact / polish

- [ ] **Hero spacing uses raw px values that bypass the SPACE token scale, breaking vertical rhythm** — design-ux · low/S · needs judgment *(verifier-adjusted)*
  - Fix: Map the genuinely off-grid hero values to the nearest tokens (14px to SPACE.sm or SPACE.md, 20px to SPACE.lg, 72px to SPACE['3xl'] or keep an intentional asymmetric pad) and reference SPACE.xl / SPACE['4xl'] for the 32px and 96px literals for consistency. Treat the 32px/96px swaps as cosmetic…
  - Evidence: Home.jsx: marginBottom '14px' (151), margin '0 auto 20px' (180), marginTop '14px' (202), marginTop '32px' (230 and 294), paddingTop '72px' / paddingBottom '96px' (88). SPACE scale in shared.jsx:38-48…
- [ ] **SupportStat note prefixes a leading em-dash glyph that reads as a dangling list marker** — design-ux · low/S · needs judgment *(verifier-adjusted)*
  - Fix: Drop the leading '— ' so the note sits as its own line (its dim color and 6px top margin already separate it from the label). Do NOT justify this as a voice-rule fix or it implies a sitewide em-dash purge that is out of scope and not what is being proposed. If a lead-in glyph is wanted, the brand ◆…
  - Evidence: Home.jsx:603 renders '— {note}'. SupportStat is used three times (Home.jsx:550-565) with notes that are complete sentences ('Agencies take weeks. We take two days.', etc.). The voice claim does not…
- [ ] **og:image is a single static card site-wide with a generic alt, on the redirecting apex host** — seo · low/S · needs judgment *(verifier-adjusted)*
  - Fix: Cheap wins: (1) change the og:image and og:image:secure_url host in index.html to www to match the host settled in finding 1; (2) make og:image:alt describe the card (e.g. 'TSD Modernization Solutions websites and AI for Charlotte businesses') rather than just the brand. Per-route OG images are a…
  - Evidence: index.html:25-30 sets a single static og:image / og:image:secure_url / og:image:alt 'TSD Modernization Solutions' on the apex host. RouteMeta (Layout.jsx:147-163) emits og:title/description/url +…
- [ ] **Stats hero panel uses the lighter decorative glass fill while its neighboring text cards use the opaque text-bearing fill** — design-ux · low/S · needs judgment *(verifier-adjusted)*
  - Fix: Start the stats-hero gradient at --glass-bg-strong (or use a flat --glass-bg-strong fill) so the panel reads as the same material as the other text cards. Frame this as material consistency; do not lead with a WCAG-AA contrast claim since the caption already sits over the opaque end of the gradient.
  - Evidence: Home.jsx:479 stats-hero background is 'linear-gradient(160deg, var(--glass-bg) 0%, var(--glass-bg-strong) 100%)'; the card is flex column with justify-content space-between (484), so the body copy at…
- [ ] **Stats 'note' lines lean on the banned contrastive/buzz cadence ('Agencies take weeks. We take two days.')** — copy-voice · low/S · needs judgment
  - Fix: Reword the 553 note to a single plain claim without the staged contrast, e.g. 'A written, fixed-price proposal two days after we talk.' Remove the em dash from the 564 note and from the hardcoded '— ' prefix at Home.jsx:603 (one component-level fix clears the decorative prefix on all notes). Keep…
  - Evidence: Confirmed: Home.jsx:553 SupportStat note='Agencies take weeks. We take two days.' (staged contrast). Home.jsx:564 note='The audit goes further — free if it can't pay for itself.' carries an em dash.…
- [ ] **Hero accent headline relies on a single visual cue that is borderline in light mode and an em-dash-free but color-only emphasis** — accessibility · low/S · needs judgment *(verifier-adjusted)*
  - Fix: No hero change. For the active nav item, if strengthening at all, add a non-color cue such as bold weight or a filled pill so 'current page' does not lean on color alone. Treat as low-value polish; the ◆ glyph + tinted background already provide a secondary cue, so 1.4.1 is likely already satisfied.
  - Evidence: Hero accent span at src/pages/Home.jsx:171. Active nav item at src/Layout.jsx:642 uses color: active ? v('accent') : v('text'), background rgba(75,156,211,0.10), plus a 9px ◆ at line 650. I…
- [ ] **Em dashes throughout visible copy violate the site's hard voice rule** — correctness · low/M · needs judgment
  - Fix: Sweep visible copy in relationships-data.js, services-data.js, and the ROUTE_META descriptions in Layout.jsx, replacing em dashes with periods/commas or rewording per the voice rules. Skip news-data.js: its 2 em dashes are both inside a code comment, not rendered text. This needs light editorial…
  - Evidence: Verified counts: relationships-data.js 13 em dashes, several in rendered copy (e.g. line 20/44/68 sub: "...Run it your way — own the source code...", and routeMetaDesc lines 12/36/60 which feed meta…
- [ ] **Stale Demo.jsx comment claims Concierge demo is unlinked, but it points to a working external app reachable only via VITE_DEMO_URL fallback** — correctness · low/S · needs judgment *(verifier-adjusted)*
  - Fix: Confirm VITE_DEMO_URL is actually set in the marketing site's Vercel build env so prod does not silently depend on the hardcoded fallback, and add a lightweight degraded state for the iframe (an onError that reveals a 'open the demo in its own tab' message). Note the page ALREADY has two 'open in…
  - Evidence: src/pages/Demo.jsx:11 DEMO_URL = import.meta.env.VITE_DEMO_URL || "https://tsd-rag-demo1.vercel.app"; Demo.jsx:69-74 <iframe src={DEMO_URL} ...> with no onError handler. Nav exposes /demo at…

## Inbox — new, unverified (from the daily heartbeat)

_empty — the 6:53am `tsd-loop-daily` run appends here_

## Shipped

_none yet — moves here when Nash merges `loop/cycle-2026-06-27`_

## Rejected — verifier killed (kept so we do not re-litigate)

- *(merged)* the SEO-lens duplicate of the canonical→www finding — same fix, shipped in cycle 1.

---

## Cycle log

| Cycle | Date | Verified | Rejected | Implemented | Branch |
|---|---|---|---|---|---|
| 1 | 2026-06-27 | 42 | 1 | 5 | `loop/cycle-2026-06-27` |
| 2 | 2026-06-27 | from cycle-1 backlog | 0 | 3 | `loop/cycle-2026-06-27` |
| 3 | 2026-06-27 | from cycle-1 backlog | 0 | 3 | `loop/cycle-2026-06-27` |
