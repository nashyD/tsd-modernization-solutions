// Empty stub for the `server-only` package under vitest. In Next.js, importing
// `server-only` throws if a module is bundled for the client; in unit tests
// there is no such boundary, so this is a no-op.
export {};
